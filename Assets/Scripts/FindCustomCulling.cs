using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class FindCustomCulling : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        //CustomParticleCulling parentCuller = FindObjectOfType<CustomParticleCulling>();
        //parentCuller.AddRenderer(GetComponent<ParticleSystem>());
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
